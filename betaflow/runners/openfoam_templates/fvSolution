FoamFile
{
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvSolution;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

solvers
{
    p
    {
        solver          GAMG;
        smoother        GaussSeidel;
        tolerance       1e-08;
        relTol          0.01;
    }

    U
    {
        solver          smoothSolver;
        smoother        symGaussSeidel;
        // Absolute tolerance at round-off: a looser value (1e-9) makes the
        // inner solve quit early once the outer residual nears it, stalling
        // outer convergence three decades above the floor. maxIter caps the
        // sweeps burnt per outer iteration once the floor is reached.
        tolerance       1e-16;
        relTol          0.01;
        maxIter         100;
    }
}

SIMPLE
{
    nNonOrthogonalCorrectors 0;
    consistent      yes;

    // All-cyclic pressure boundaries: fix the level at one cell.
    pRefCell        0;
    pRefValue       0;

    // No residualControl: Uy and p are identically zero in this flow, so
    // their NORMALISED residuals never drop and the criterion is unmeetable.
    // The runner gates convergence on the final Ux residual parsed from the
    // log instead.
}

relaxationFactors
{
    equations
    {
        U               0.9;
        ".*"            0.9;
    }
}

// ************************************************************************* //
