[project]
name = "betaflow"
version = "0.1.0"
description = "Solver-agnostic validation harness for haemodynamic CFD"
requires-python = ">=3.10"
dependencies = ["numpy", "pyyaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
